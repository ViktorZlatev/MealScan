specVersion: "v1beta"
serviceId: "scanapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "scanapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
